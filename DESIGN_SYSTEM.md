# 基金时光机设计系统

版本：V8.3
适用范围：基金时光机全部页面与后续新增功能

## 1. 产品视觉定位

基金时光机是面向年轻投资者的成长训练平台，不是行情后台，也不是短期营销活动页。

视觉关键词：

- 温柔
- 专业
- 高级
- 治愈
- 成长感
- 轻金融
- 长期陪伴

设计原则：

1. 内容先于装饰：首先让用户看清今天要做什么、为什么做、做完有什么反馈。
2. 低刺激：避免高饱和色、大面积纯色和强烈黑白对比。
3. 一套组件：新功能必须复用现有卡片、按钮、表单和反馈组件。
4. 成长而非收益：等级、记录、复盘和习惯是主要视觉反馈，收益数字不制造焦虑。
5. 移动优先：所有功能在手机端必须有可访问入口。

## 2. 配色

### 基础色

| 用途 | 变量 | 色值 |
|---|---|---|
| 页面背景 | `--bg` | `#F6F7F9` |
| 暖色背景 | `--bg-warm` | `#FAF8F5` |
| 主文字 | `--text` | `#28384F` |
| 次级文字 | `--text-soft` | `#4F6078` |
| 辅助文字 | `--muted` | `#7D8A9E` |
| 轻边框 | `--line` | `rgba(111,128,157,.16)` |

### 品牌与马卡龙色

| 用途 | 变量 | 色值 |
|---|---|---|
| 主色 | `--primary` | `#8E8AD4` |
| 深主色 | `--primary-deep` | `#6F6AAE` |
| 浅紫 | `--lilac` | `#E9E6F8` |
| 雾蓝 | `--blue` | `#DEEBF7` |
| 淡粉 | `--pink` | `#F6E5EA` |
| 薄荷绿 | `--mint` | `#DFF0E8` |
| 暖米色 | `--cream` | `#F4EEE2` |

### 语义规则

- 紫色：品牌、当前步骤、成长等级。
- 雾蓝：信息、学习、市场情境。
- 薄荷绿：完成、正向进度、纪律建立。
- 淡粉：提醒、情绪、温和的风险提示。
- 红色只用于必要的风险或删除操作，不作为大面积背景。
- 禁止重新引入高饱和紫色、橙黄色大色块。

## 3. 字体

字体栈：

```css
Inter, -apple-system, BlinkMacSystemFont, "SF Pro Display",
"PingFang SC", "Microsoft YaHei", sans-serif
```

字号建议：

| 场景 | 字号 |
|---|---|
| 首页主标题 | `42–72px` 自适应 |
| 页面标题 | `30–46px` 自适应 |
| 模块标题 | `19–25px` |
| 正文 | `15px` |
| 辅助正文 | `13–14px` |
| 标签 | `11–12px` |

字重规则：

- 主标题：700–760
- 模块标题：680–720
- 正文：400–500
- 标签和按钮：620–700
- 不在大面积内容中使用 800 以上字重

## 4. 圆角、边框与阴影

圆角：

| 组件 | 圆角 |
|---|---|
| 输入框、标签 | `12–14px` |
| 小卡片 | `16px` |
| 标准功能卡 | `20px` |
| 大页面容器、Hero | `24–34px` |

边框：

```css
1px solid rgba(111,128,157,.16)
```

阴影：

```css
--shadow-sm: 0 8px 24px rgba(76,91,119,.07);
--shadow-md: 0 18px 52px rgba(76,91,119,.10);
```

禁止使用纯黑重阴影或高反差描边。

## 5. 玻璃卡片规范

标准玻璃卡片：

```css
background: rgba(255,255,255,.72);
border: 1px solid rgba(111,128,157,.16);
backdrop-filter: blur(22px) saturate(120%);
box-shadow: var(--shadow-sm);
border-radius: 20px;
```

使用规则：

- 页面级模块、数据卡、功能入口、日志和复盘都使用同一基础表面。
- 区分模块时使用很浅的马卡龙渐变，不使用整块高饱和底色。
- 卡片内边距优先使用 `18px`、`22px`、`28px`。
- 同一组卡片保持一致高度、圆角和标题位置。

## 6. 组件规范

### 导航

- 桌面端使用顶部玻璃导航。
- 移动端使用五项底部 App 导航。
- 次要功能统一进入“更多”，禁止因响应式布局隐藏功能入口。

### 按钮

- 主按钮：低饱和紫色到雾蓝渐变。
- 次按钮：浅紫透明背景，无重阴影。
- 风险按钮：淡粉背景和深粉文字。
- 同一模块只保留一个主要行动按钮。

### 数据卡

- 标签位于顶部，核心数字位于下方。
- 数字不使用超过两种强调颜色。
- 收益数据必须标注“模拟”或“仅用于训练”。

### 功能入口卡

- 使用统一玻璃表面。
- 包含一个柔和色图标区、标题和一句说明。
- 整张卡片可点击，避免额外堆叠普通按钮。

### 表单

- 输入框使用半透明白色背景、12px 圆角和轻边框。
- 错误状态使用淡粉描边，不使用高饱和红色填充。
- 复杂流程使用真实步骤，不用装饰性假进度。

### 反馈与复盘

- 先展示结论，再展示证据。
- 正向完成使用薄荷绿。
- 情绪提示使用淡粉。
- 行为改进建议使用低饱和信息卡，不使用警告式大色块。

## 7. 动效

允许：

- 页面进入：透明度与 `10px` 以内的轻微上移。
- 卡片悬停：最多上移 `5px`。
- 进度条：宽度平滑变化。
- 柔和装饰元素：缓慢浮动。

时长：

- 交互动效：`180–220ms`
- 页面进入：`400–500ms`
- 装饰浮动：`6–8s`

必须支持 `prefers-reduced-motion`，用户选择减少动画时关闭非必要动效。

## 8. 后续开发约束

以后新增功能必须遵守：

1. 不重新定义新的主色或卡片风格。
2. 不为单一页面创建一套独立视觉语言。
3. 优先复用 `.card`、`.box`、`.feature-card`、`.v8-metric`、`.btn-secondary` 等已有组件。
4. 新页面必须同时设计桌面与移动布局。
5. 不删除或隐藏已有功能入口。
6. 新增视觉状态必须使用本文件定义的语义颜色。
7. 若现有组件无法满足需求，先扩展通用组件，再应用到具体功能。

未经明确的产品级视觉重构需求，不得重新设计基金时光机 UI。

## 9. V8.3 首页构图与插画规范

- 首屏采用「成长欢迎区 + 今日任务卡」的双栏 App 构图，桌面端比例约为 `1.6 : 0.7`。
- 三项核心数据必须嵌入成长欢迎区，不能拆成互不相关的后台数据模块。
- 主视觉使用原创的低饱和成长旅程插画，内容服务于“长期训练”，不得使用行情大屏或营销海报式素材。
- 功能入口使用同一套柔和立体插画语言：市场曲线、基金组合、记录纸张、复盘雷达。
- 桌面端保留顶部主导航与首页快捷 Dock；移动端只保留底部五项主导航，避免重复占用空间。
- 插画不能承载必要文字信息；关闭图片或减少动画后，标题、数据、按钮和功能入口仍须完整可用。
- 后续替换主视觉时，保持暖白、雾蓝、浅紫、淡粉、薄荷绿的低饱和色域，并保留足够文字留白。

